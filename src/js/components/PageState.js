/**
 * Details the page state of the component. Has
 * three states -- loading, ready, and not found.
 */

const PAGE_STATE = {
  LOADING: "loading",
  READY: "ready",
  NOT_FOUND: "not_found"
}

export default PAGE_STATE;